var main = require('./main')


exports.poll =  async function (message)  {
    if (message.chatId.endsWith("@g.us") & message.body !== "!result" & message.body !== "!poll") {

            
        if (polllist.indexOf(message.chatId) > -1) {

            stimmen.push

        } else {
        }
            
    } 
    if (message.body === "!result") {

            

    }

}